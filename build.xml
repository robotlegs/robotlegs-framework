<!-- 
	For the most detail when running this build, call "ant -v clean package".
 -->
<project name="robotlegs-framework" basedir="." default="package">
	<!-- setup a prefix for all environment variables -->
	<property file="build.properties" />

	<!-- Setup Flex and FlexUnit ant tasks -->
	<property name="FLEX_HOME" location="${sdk.loc}" />
	<taskdef resource="flexTasks.tasks" classpath="${lib.loc}/flexTasks.jar" />
	<taskdef resource="flexUnitTasks.tasks" classpath="${lib.loc}/flexUnitTasks.jar" />

	<target name="clean">
		<!-- Remove all directories created during the build process -->
		<delete includeemptydirs="true" failonerror="false">
			<fileset dir="${bin.loc}" defaultexcludes="false">
				<include name="**/*" />
			</fileset>
			<fileset dir="${report.loc}" defaultexcludes="false">
				<include name="**/*" />
			</fileset>
			<fileset dir="${dist.loc}" defaultexcludes="false">
				<include name="**/*" />
			</fileset>
		</delete>
	</target>

	<target name="init">
		<!-- Create directories needed for the build process -->
		<mkdir dir="${bin.loc}" />
		<mkdir dir="${report.loc}" />
		<mkdir dir="${dist.loc}" />
	</target>
	
	<target name="compile" depends="init"> 
        <compc output="${bin.loc}/robotlegs-framework.swc">  
            <!-- include our Class packages into the build (com folder) -->  
            <include-sources dir="${main.src.loc}/org" includes="*" />  
			<library-path dir="${lib.loc}" append="true">
				<include name="*.swc" />
			</library-path>
			<library-path dir="${swift.suspenders.loc}" append="true">
				<include name="*.swc" />
			</library-path>
			<compiler.verbose-stacktraces>true</compiler.verbose-stacktraces>
			<compiler.headless-server>true</compiler.headless-server>
        </compc>  
        <echo>SWC created successfully</echo>  
	</target>  
	
	<target name="test" depends="compile">
		<!-- Compile TestRunner.mxml as a SWF -->
		<mxmlc file="${test.src.loc}/RobotlegsTest.mxml" output="${bin.loc}/TestRunner.swf">
			<source-path path-element="${main.src.loc}" />
			<library-path dir="${lib.loc}" append="true">
				<include name="*.swc" />
			</library-path>
			<library-path dir="${swift.suspenders.loc}" append="true">
				<include name="*.swc" />
			</library-path>
			<compiler.verbose-stacktraces>true</compiler.verbose-stacktraces>
			<compiler.headless-server>true</compiler.headless-server>
		</mxmlc>

		<!-- Execute TestRunner.swf as FlexUnit tests and publish reports -->
		<flexunit swf="bin/TestRunner.swf" toDir="${report.loc}" haltonfailure="false" verbose="true" localTrusted="true" />

		<!-- Generate readable JUnit-style reports -->
		<junitreport todir="${report.loc}">
			<fileset dir="${report.loc}">
				<include name="TEST-*.xml" />
			</fileset>
			<report format="frames" todir="${report.loc}/html" />
		</junitreport>
	</target>

	<target name="package" depends="test">
		<!-- Assemble final website -->
		<copy file="${bin.loc}/robotlegs-framework.swc" todir="${dist.loc}" />

		<!-- Zip up final website -->
		<zip destfile="${dist.loc}/${ant.project.name}.${build.version}.zip">
			<fileset dir="${dist.loc}" />
		</zip>
	</target>
</project>